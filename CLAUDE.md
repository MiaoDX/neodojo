# CLAUDE.md

Read `AGENTS.md` first. It contains the shared repo guidance for Claude, Codex, and other coding agents.

Claude-specific notes:

- Keep durable project guidance in `AGENTS.md`, `CLAUDE.md`, or future `docs/agents/**` files, not in ignored `.claude/` local artifacts.
- There are no checked-in Claude commands, hooks, or routines yet. Treat this as a plain bootstrap repo unless such files are added.
- If this file grows beyond Claude-specific deltas, collapse shared rules back into `AGENTS.md` and keep this file as a pointer.
